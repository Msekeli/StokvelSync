@page "/admin/dashboard"
@using StokvelSync.Shared
@inject HttpClient Http

<div class="container-fluid py-4">
    <h2 class="fw-bold mb-4" style="color: var(--stokvel-dark);">Admin Control Center</h2>

    <div class="card border-0 shadow-sm">
        <div class="card-body p-0">
            @* We avoid standard HTML tables for responsiveness and your preference *@
            @foreach (var m in allMembers ?? new())
            {
                <div class="p-3 border-bottom d-flex flex-wrap justify-content-between align-items-center">
                    <div class="col-12 col-md-4">
                        <h6 class="mb-0 fw-bold">@m.FullName</h6>
                        <small class="text-muted">@m.Email</small>
                    </div>
                    <div class="col-6 col-md-3">
                        <a href="https://wa.me/@m.WhatsAppNumber" target="_blank" class="btn btn-sm btn-success">
                            <i class="bi bi-whatsapp"></i> @m.WhatsAppNumber
                        </a>
                    </div>
                    <div class="col-6 col-md-2 text-end">
                        <span class="d-block small text-muted">Total Saved</span>
                        <span class="fw-bold">R @m.TotalContribution</span>
                    </div>
                    <div class="col-12 col-md-2 text-md-end mt-2 mt-md-0">
                        <button class="btn btn-sm btn-outline-primary">Manage</button>
                    </div>
                </div>
            }
        </div>
    </div>
</div>

@code {
    private List<Member>? allMembers;
    protected override async Task OnInitializedAsync() => 
        allMembers = await Http.GetFromJsonAsync<List<Member>>("GetMembers");
}